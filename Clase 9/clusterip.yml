apiVersion: v1
kind: Service
metadata:
  name: ejemplo-clusterip
  labels:
    app: ejemplo
spec:
  type: ClusterIP
  selector:
    app: ejemplo
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: http
